cut -b3-
# Interpreter for 'vpn-switch logs validate' command
# Check for old log files beyond the retention period
#
# Default value: "cut -b3-"
#   Strips the leading '# ' comment marker from diagnostic output
#   Display-only function (no execution, no privileged operations)
#
# All checks run at generation time; the function emits final data
# ("# "-prefixed report lines) and returns non-zero on errors, which
# propagates through the producer exit channel regardless of the
# interpreter (see combine_exit_codes).
#
# Example usage:
#   vpn-switch logs validate
#
# See ARCHITECTURE.md for details on terminal function pattern
